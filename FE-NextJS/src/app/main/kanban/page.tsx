import Container from '@mui/material/Container';
import Typography from '@mui/material/Typography';
import { memo } from 'react';

const Kanban = () => {
  return (
    <Container>
      <Typography>Kanban page</Typography>
    </Container>
  );
};

export default memo(Kanban);
