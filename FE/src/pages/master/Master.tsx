import { Box, Container } from '@mui/material';
import { memo } from 'react';

const Master = () => {
  return (
    <Container>
      <Box>Master page</Box>
    </Container>
  );
};

export default memo(Master);
