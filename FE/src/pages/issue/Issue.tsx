import { Box, Container } from '@mui/material';
import { memo } from 'react';

const Issue = () => {
  return (
    <Container>
      <Box>Issue page</Box>
    </Container>
  );
};

export default memo(Issue);
