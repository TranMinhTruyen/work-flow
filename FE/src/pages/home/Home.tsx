import { Box, Container } from '@mui/material';
import { memo } from 'react';

const Home = () => {
  return (
    <Container>
      <Box>Home page</Box>
    </Container>
  );
};

export default memo(Home);
